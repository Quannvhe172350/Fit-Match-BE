name: CI/CD

on:
  push:
    branches: ["**"]
  pull_request:
    branches: [master, develop]

env:
  REGISTRY: ghcr.io

jobs:
  test:
    name: Build
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Set up Java 21
        uses: actions/setup-java@v4
        with:
          java-version: "21"
          distribution: temurin
          cache: maven

      - name: Build
        run: mvn package -DskipTests -B

  build-and-push:
    name: Build & Push Docker Image
    runs-on: ubuntu-latest
    needs: test
    if: github.ref == 'refs/heads/develop' && github.event_name == 'push'
    permissions:
      contents: read
      packages: write

    outputs:
      image: ${{ steps.meta.outputs.tags }}

    steps:
      - uses: actions/checkout@v4

      - name: Set lowercase image name
        run: echo "IMAGE_NAME=$(echo ${GITHUB_REPOSITORY} | tr '[:upper:]' '[:lower:]')" >> $GITHUB_ENV

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Extract Docker metadata
        id: meta
        uses: docker/metadata-action@v5
        with:
          images: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}
          tags: |
            type=sha,prefix=sha-
            type=raw,value=latest

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ steps.meta.outputs.tags }}
          labels: ${{ steps.meta.outputs.labels }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    name: Deploy to GCE
    runs-on: ubuntu-latest
    needs: build-and-push
    environment: dev

    steps:
      - name: Authenticate to Google Cloud
        uses: google-github-actions/auth@v2
        with:
          credentials_json: ${{ secrets.GCP_SA_KEY }}

      - name: Set up gcloud
        uses: google-github-actions/setup-gcloud@v2

      - name: Write env file and copy to VM
        run: |
          cat > /tmp/fitmatch.env << EOF
          SPRING_PROFILES_ACTIVE=dev
          DB_URL=${{ secrets.DB_URL }}
          DB_USERNAME=${{ secrets.DB_USERNAME }}
          DB_PASSWORD=${{ secrets.DB_PASSWORD }}
          JWT_SECRET=${{ secrets.JWT_SECRET }}
          APP_BASE_URL=${{ secrets.APP_BASE_URL }}
          FRONTEND_URL=${{ secrets.FRONTEND_URL }}
          # Storage: ảnh nằm trên Google Cloud Storage, KHÔNG trên đĩa container
          # (đĩa container bị xoá mỗi lần redeploy). GCS_ENABLED viết tường minh dù
          # application-dev.yml đã bật sẵn — đọc file này phải thấy được là đang
          # dùng bucket nào, không phải lần mò trong YAML profile.
          GCS_ENABLED=true
          GCS_BUCKET=${{ secrets.GCS_BUCKET }}
          GCP_PROJECT_ID=${{ secrets.GCP_PROJECT_ID }}
          # Bucket được cấp allUsers:objectViewer -> trả URL công khai cố định
          # (cache được ở CDN). Đổi bucket sang private thì PHẢI đổi cờ này thành
          # false, nếu không mọi ảnh sẽ 403 trên trình duyệt.
          MEDIA_PUBLIC_READ=true
          # Dùng `|| 'x'`: vars chưa khai báo sẽ render thành chuỗi RỖNG, mà env
          # rỗng vẫn là "đã định nghĩa" nên default trong application.yml không cứu
          # -> Spring bind "" vào int và app chết lúc khởi động.
          MAX_IMAGE_SIZE_MB=${{ vars.MAX_IMAGE_SIZE_MB || '5' }}
          MAX_IMAGES_PER_ENTITY=${{ vars.MAX_IMAGES_PER_ENTITY || '10' }}
          # Hạn mức tầng servlet PHẢI lớn hơn MAX_IMAGE_SIZE_MB: nhỏ hơn thì request
          # bị chặn trước khi vào controller và người dùng nhận lỗi kỹ thuật thay vì
          # thông báo "ảnh vượt quá N MB".
          MAX_UPLOAD_FILE_SIZE=${{ vars.MAX_UPLOAD_FILE_SIZE || '12MB' }}
          MAX_UPLOAD_REQUEST_SIZE=${{ vars.MAX_UPLOAD_REQUEST_SIZE || '80MB' }}
          MAIL_HOST=smtp.gmail.com
          MAIL_PORT=587
          MAIL_USERNAME=${{ secrets.MAIL_USERNAME }}
          MAIL_PASSWORD=${{ secrets.MAIL_PASSWORD }}
          MAIL_FROM=${{ secrets.MAIL_FROM }}
          GOOGLE_APPLICATION_CREDENTIALS=/app/gcp-sa.json
          CORS_ALLOWED_ORIGINS=${{ secrets.FRONTEND_URL }}
          CASSO_WEBHOOK_SECRET=${{ secrets.CASSO_WEBHOOK_SECRET }}
          VIETQR_BANK_BIN=${{ secrets.VIETQR_BANK_BIN }}
          VIETQR_ACCOUNT_NO=${{ secrets.VIETQR_ACCOUNT_NO }}
          VIETQR_ACCOUNT_NAME=${{ secrets.VIETQR_ACCOUNT_NAME }}
          GOOGLE_OAUTH_CLIENT_IDS=${{ secrets.GOOGLE_OAUTH_CLIENT_IDS }}
          EOF

          cat > /tmp/gcp-sa.json << 'SAEOF'
          ${{ secrets.GCP_SA_KEY }}
          SAEOF

          gcloud compute scp /tmp/fitmatch.env ${{ secrets.GCE_INSTANCE }}:/tmp/fitmatch.env \
            --zone=${{ secrets.GCE_ZONE }} \
            --project=${{ secrets.GCE_PROJECT }} \
            --quiet

          gcloud compute scp /tmp/gcp-sa.json ${{ secrets.GCE_INSTANCE }}:/tmp/gcp-sa.json \
            --zone=${{ secrets.GCE_ZONE }} \
            --project=${{ secrets.GCE_PROJECT }} \
            --quiet

      - name: Deploy via gcloud SSH
        run: |
          IMAGE=$(echo "ghcr.io/${GITHUB_REPOSITORY}:latest" | tr '[:upper:]' '[:lower:]')
          gcloud compute ssh ${{ secrets.GCE_INSTANCE }} \
            --zone=${{ secrets.GCE_ZONE }} \
            --project=${{ secrets.GCE_PROJECT }} \
            --quiet \
            --command="
              echo '${{ secrets.GHCR_PAT }}' | sudo docker login ghcr.io -u ${{ secrets.GHCR_USERNAME }} --password-stdin &&
              sudo docker pull ${IMAGE} &&
              sudo docker stop fitmatch || true &&
              sudo docker rm fitmatch || true &&
              sudo mkdir -p /opt/fitmatch &&
              sudo mv /tmp/gcp-sa.json /opt/fitmatch/gcp-sa.json &&
              sudo chmod 600 /opt/fitmatch/gcp-sa.json &&
              sudo docker run -d \
                --name fitmatch \
                --restart unless-stopped \
                --network=host \
                --env-file /tmp/fitmatch.env \
                -v /opt/fitmatch/gcp-sa.json:/app/gcp-sa.json:ro \
                ${IMAGE} &&
              sudo docker image prune -f &&
              rm /tmp/fitmatch.env
            "
