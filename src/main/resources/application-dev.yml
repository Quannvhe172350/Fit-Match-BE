spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/fitmatch_dev?createDatabaseIfNotExist=true
    username: root
    password: root
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

logging:
  level:
    com.fitmatch: DEBUG
    org.hibernate.SQL: DEBUG
